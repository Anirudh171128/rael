fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1
apscheduler==3.11.0
anthropic==0.42.0
openai==1.59.6
google-genai==0.8.0
websockets==14.1
greenlet==3.1.1
pyyaml==6.0.2
# Discovery engine.
# Tavily — preferred live search executor (real companies, no scraping). Set TAVILY_API_KEY.
tavily-python==0.5.0
# Playwright — headless-browser fallback. After install, fetch the browser:
#   playwright install chromium
# Absent both a Tavily key and the browser, discovery degrades to a deterministic mock.
playwright==1.60.0
